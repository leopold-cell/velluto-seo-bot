anthropic
openai
requests
python-dotenv
ddgs
praw
PyYAML
playwright
# Optional — only needed for the Google Drive hosting path (drive_upload.py).
# Safe to skip; drive_upload no-ops and the reel falls back to the raw Higgsfield URL.
google-api-python-client
google-auth
